import React from "react";

export const Team = (props) => {
  return (
    <div id="team" className="text-center">
      <div className="container">
        <div className="col-md-8 col-md-offset-2 section-title">
          <h2>Conoce a nuestro equipo</h2>
          <p>
          Conoce al equipo de expertos detrás de nuestra empresa: mentes apasionadas y comprometidas en impulsar el progreso y la excelencia en cada proyecto
          </p>
        </div>
        <div id="row" className="col-md-12 col-md-offset-3 section-title">
          {props.data
            ? props.data.map((d, i) => (
                <div key={`${d.name}-${i}`} className="col-md-3 col-sm-6 team">
                  <div className="thumbnail">
                    {" "}
                    <img src={d.img} alt="..." className="team-img" />
                    <div className="caption">
                      <h4>{d.name}</h4>
                      <p>{d.job}</p>
                    </div>
                  </div>
                </div>
              ))
            : "loading"}
        </div>
      </div>
    </div>
  );
};
